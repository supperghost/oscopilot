Linux 下恶意程序（病毒、木马、Rootkit）隐藏进程的核心目标是：**让用户态工具（ps/top/systemd/procfs）看到的进程视图与内核真实状态不一致**。

如果你设计通用病毒发现软件，建议不要只依赖 `/proc`，而应该建立**多视角进程枚举 + 一致性校验**体系。

下面总结 Linux 常见进程隐藏技术，以及对应检测逻辑。

---

# 1. 修改 procfs 隐藏进程（用户态 Rootkit）

## 原理

正常：

```
ps
 |
procps
 |
读取 /proc
 |
/proc/<pid>
```

Rootkit 通过：

* LD_PRELOAD
* 替换 ps/top
* 修改 libproc
* hook getdents()

隐藏：

```
/proc/12345
```

导致：

```
ps aux
```

看不到。

---

## 检测逻辑

### 方法1：直接扫描 /proc

不要调用 ps。

代码逻辑：

```text
for entry in /proc:
    if entry is number:
        pid = entry

        read:
          /proc/pid/status
          /proc/pid/cmdline
          /proc/pid/exe

        建立进程列表
```

例如：

```bash
ls /proc | grep ^[0-9]
```

---

### 方法2：ps 与 /proc 对比

执行：

```bash
ps -ef
```

得到：

```
PID list A
```

自行扫描：

```
PID list B
```

差异：

```
B - A
```

例如：

```
发现：

/proc/23456

但是ps没有

```

高度可疑。

---

## 误报

可能原因：

* ps版本异常
* namespace隔离
* 容器PID namespace

因此需要继续验证。

---

# 2. getdents 系统调用隐藏（最常见 Rootkit）

## 原理

这是 Linux Rootkit 最经典方法。

正常：

```
ls
 |
readdir()
 |
getdents()
 |
VFS
 |
目录项
```

Rootkit hook:

```
sys_getdents64()

过滤:

if pid == hidden_pid:
    remove entry
```

效果：

```
ls /proc

看不到PID
```

但是：

```
内核task_struct

仍然存在
```

---

# 检测方法1：系统调用旁路扫描

不要通过目录读取。

使用：

## 方法A：BPF trace task_struct

eBPF：

tracepoint:

```
sched_process_exec
sched_process_exit
```

维护：

```
真实运行PID集合
```

例如：

```
exec事件:

PID=1234

加入map


exit事件:

删除

```

然后：

比较：

```
BPF map PID

vs

/proc PID
```

发现：

```
BPF存在

/proc不存在

=> 隐藏进程
```

---

## 方法B：遍历内核task list

需要：

* 内核模块
* crash
* kprobe

内核结构：

```
task_struct
 |
tasks list
```

遍历：

```
for_each_process(task)
{
 pid = task->pid
}
```

得到：

```
kernel PID list
```

和：

```
/proc
```

比较。

这是 Rootkit 检测最可靠方式。

---

# 3. 内核模块 Rootkit 隐藏进程

## 原理

恶意模块：

```
evil.ko
```

加载：

```
insmod evil.ko
```

hook：

* sys_getdents
* proc_pid_readdir
* tasklist

甚至修改：

```
task_struct
```

---

# 检测逻辑

## 检查模块

```bash
lsmod
```

比较：

```
/sys/module
```

和：

```
/proc/modules
```

异常：

```
module存在
但是lsmod没有
```

---

## 检测未注册模块

扫描：

```
/sys/module/*
```

例如：

```
ls /sys/module
```

发现：

```
xyaudit
```

但是：

```
modprobe -r xyaudit
```

异常。

---

# 4. PID namespace隐藏（容器常见）

## 原理

容器：

```
host:

PID 5000

container:

PID 1

```

容器内：

```
ps
```

看不到宿主进程。

---

# 检测

读取：

```
/proc/<pid>/status
```

关注：

```
NSpid
```

例如：

```
NSpid:
1 5000
```

说明：

```
namespace映射
```

---

检查：

```bash
lsns -t pid
```

发现：

```
多个PID namespace
```

---

# 5. 伪装进程名

## 方法

修改：

```
argv[0]
```

例如：

真实：

```
/tmp/xmrig
```

显示：

```
[kworker/u8:2]
```

---

# 检测

## exe 与 cmdline 比较

读取：

```
/proc/pid/exe
```

例如：

```
/tmp/.abc/xmrig
```

但是：

```
cmdline:

[kworker/u8:2]
```

异常。

---

规则：

```
basename(exe)

!=

argv[0]

```

提高风险。

---

# 6. 删除可执行文件

## 原理

启动：

```
./malware
```

随后：

```
unlink malware
```

进程继续。

显示：

```
/proc/pid/exe

/tmp/malware (deleted)

```

---

检测：

扫描：

```
/proc/*/exe
```

如果：

```
(deleted)
```

风险高。

例如：

```bash
readlink /proc/1234/exe

/tmp/.x/a.out (deleted)
```

---

# 7. LD_PRELOAD 用户态隐藏

## 原理

修改：

```
/etc/ld.so.preload
```

例如：

```
/lib/libhide.so
```

所有动态程序：

```
加载恶意so
```

---

检测：

检查：

```bash
cat /etc/ld.so.preload
```

规则：

```
非系统库
+
未知owner
+
异常路径

```

风险。

---

同时检查：

```bash
env | grep LD_PRELOAD
```

---

# 8. ptrace 注入隐藏

## 原理

攻击：

```
正常进程

+
ptrace

注入shellcode

```

例如：

```
sshd

里面运行恶意代码

```

---

检测：

查看：

```
/proc/pid/status
```

字段：

```
TracerPid
```

例如：

```
TracerPid: 1234
```

非调试场景：

异常。

---

# 9. 内存驻留型木马

没有文件：

```
/tmp删除
```

代码：

```
anonymous mmap
```

---

检测：

读取：

```
/proc/pid/maps
```

关注：

异常：

```
rwxp
```

例如：

```
7f000000 rwxp
```

匿名可执行内存。

---

规则：

```
maps:

[rwx]

AND

pathname为空

```

高风险。

---

# 10. 进程父子关系伪装

木马：

```
systemd
 |
sshd
 |
bash
 |
malware
```

或者：

伪装：

```
/usr/bin/update
```

---

检测：

建立进程树：

```
PID
 |
PPID
 |
exe
```

异常：

例如：

```
nginx
 |
bash
 |
curl
 |
chmod
```

---

# 11. Socket隐藏

木马监听：

```
0.0.0.0:4444
```

但是：

```
netstat
```

隐藏。

---

检测：

不要依赖netstat。

读取：

```
/proc/net/tcp
```

或者：

eBPF:

```
tcp_v4_listen
```

记录：

```
socket inode
```

关联：

```
/proc/pid/fd
```

---

# 12. 时间隐藏

修改：

```
进程启动时间
```

或者：

伪装：

```
uptime
```

---

检测：

读取：

```
/proc/pid/stat

starttime
```

和：

```
文件mtime
```

比较。

---

# 推荐检测架构

设计一个类似商业EDR的模型：

```
                 Kernel

              eBPF Probe
                  |
        +---------+---------+
        |                   |
   process exec        socket event
        |
        |
      PID Map
        |
        |
-------------------------------
        用户态Agent
-------------------------------

 /proc扫描

 ps扫描

 mount扫描

 module扫描

 file hash扫描


        |
        |
    Risk Engine

```

---

# 核心检测规则优先级

| 检测                        | 覆盖             | 推荐    |
| ------------------------- | -------------- | ----- |
| eBPF exec/exit追踪 vs /proc | 隐藏进程           | ★★★★★ |
| /proc扫描                   | 普通隐藏           | ★★★★★ |
| task_struct遍历             | Kernel Rootkit | ★★★★★ |
| exe deleted检测             | 木马             | ★★★★  |
| maps匿名rwx                 | 内存木马           | ★★★★  |
| ld.so.preload             | 用户态rootkit     | ★★★★  |
| 异常父子关系                    | 木马行为           | ★★★   |
| socket关联                  | C2             | ★★★★  |
| namespace检测               | 容器环境           | ★★★★  |

---

# 如果做通用Linux病毒发现软件，我建议核心能力分三层

## 第一层：进程发现

目标：

> "系统真实运行了什么"

实现：

* eBPF process tracker
* /proc scanner
* namespace aware

---

## 第二层：隐藏检测

目标：

> "什么东西故意不让我看到"

实现：

* PID差异
* module差异
* syscall hook检测
* task_struct扫描

---

## 第三层：行为检测

目标：

> "即使没有隐藏，也判断是否恶意"

实现：

* 异常网络
* 文件删除
* privilege escalation
* mmap executable
* shell spawn

---

如果目标是达到 **Linux版Rootkit Hunter / chkrootkit + EDR级别**，重点应该优先实现：

1. **eBPF进程生命周期追踪**
2. **/proc与kernel task list一致性检测**
3. **syscall hook完整性检测**
4. **进程内存扫描**

这四个覆盖当前Linux Rootkit绝大多数隐藏方式。

